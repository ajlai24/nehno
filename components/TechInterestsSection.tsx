import { Container, Typography } from '@mui/material';
import Section from './Section';

const TechInterestsSection = () => (
  <Section>
    <Container>
      <Typography>
        React, Storybook, Webpack, Express, Typescript, and GraphQL
      </Typography>
    </Container>
  </Section>
);

export default TechInterestsSection;
